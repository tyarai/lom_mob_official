
class AlertDialog {
/*
  static const AlertAction _close = AlertAction(text: "Close", onPressed: () {
      return;
  });


  static const List<AlertAction> defaultActions = [AlertDialog._close];

  static void show(BuildContext context,String message,{String title = "", List<AlertAction> actions =  AlertDialog.defaultActions}){
    showAlert(
      context: context,
      title: title,
      body: message,
      actions: actions,
    );

  }
  */
}